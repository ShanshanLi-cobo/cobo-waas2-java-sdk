/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.StellarContractCallContractType;
import com.cobo.waas2.model.StellarContractCallTrustLineOperationType;
import com.cobo.waas2.model.StellarContractCallTrustLineParam;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.cobo.waas2.JSON;

@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class StellarContractCallContractParam extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(StellarContractCallContractParam.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!StellarContractCallContractParam.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'StellarContractCallContractParam' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<StellarContractCallTrustLineParam> adapterStellarContractCallTrustLineParam = gson.getDelegateAdapter(this, TypeToken.get(StellarContractCallTrustLineParam.class));

            return (TypeAdapter<T>) new TypeAdapter<StellarContractCallContractParam>() {
                @Override
                public void write(JsonWriter out, StellarContractCallContractParam value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `StellarContractCallTrustLineParam`
                    if (value.getActualInstance() instanceof StellarContractCallTrustLineParam) {
                        JsonElement element = adapterStellarContractCallTrustLineParam.toJsonTree((StellarContractCallTrustLineParam)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: StellarContractCallTrustLineParam");
                }

                @Override
                public StellarContractCallContractParam read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    JsonObject jsonObject = jsonElement.getAsJsonObject();

                    // use discriminator value for faster oneOf lookup
                    StellarContractCallContractParam newStellarContractCallContractParam = new StellarContractCallContractParam();
                    if (jsonObject.get("contract_type") == null) {
                        log.log(Level.WARNING, "Failed to lookup discriminator value for StellarContractCallContractParam as `contract_type` was not found in the payload or the payload is empty.");
                    } else  {
                        // look up the discriminator value in the field `contract_type`
                        switch (jsonObject.get("contract_type").getAsString()) {
                            case "TrustLine":
                                deserialized = adapterStellarContractCallTrustLineParam.fromJsonTree(jsonObject);
                                newStellarContractCallContractParam.setActualInstance(deserialized);
                                return newStellarContractCallContractParam;
                            case "StellarContractCallTrustLineParam":
                                deserialized = adapterStellarContractCallTrustLineParam.fromJsonTree(jsonObject);
                                newStellarContractCallContractParam.setActualInstance(deserialized);
                                return newStellarContractCallContractParam;
                            default:
                                log.log(Level.WARNING, String.format("Failed to lookup discriminator value `%s` for StellarContractCallContractParam. Possible values: TrustLine StellarContractCallTrustLineParam", jsonObject.get("contract_type").getAsString()));
                        }
                    }

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize StellarContractCallTrustLineParam
                    try {
                        // validate the JSON object to see if any exception is thrown
                        StellarContractCallTrustLineParam.validateJsonElement(jsonElement);
                        actualAdapter = adapterStellarContractCallTrustLineParam;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'StellarContractCallTrustLineParam'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for StellarContractCallTrustLineParam failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'StellarContractCallTrustLineParam'", e);
                    }

                    if (match == 1) {
                        StellarContractCallContractParam ret = new StellarContractCallContractParam();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for StellarContractCallContractParam: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public StellarContractCallContractParam() {
        super("oneOf", Boolean.FALSE);
    }

    public StellarContractCallContractParam(StellarContractCallTrustLineParam o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("StellarContractCallTrustLineParam", StellarContractCallTrustLineParam.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return StellarContractCallContractParam.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * StellarContractCallTrustLineParam
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof StellarContractCallTrustLineParam) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be StellarContractCallTrustLineParam");
    }

    /**
     * Get the actual instance, which can be the following:
     * StellarContractCallTrustLineParam
     *
     * @return The actual instance (StellarContractCallTrustLineParam)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `StellarContractCallTrustLineParam`. If the actual instance is not `StellarContractCallTrustLineParam`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `StellarContractCallTrustLineParam`
     * @throws ClassCastException if the instance is not `StellarContractCallTrustLineParam`
     */
    public StellarContractCallTrustLineParam getStellarContractCallTrustLineParam() throws ClassCastException {
        return (StellarContractCallTrustLineParam)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to StellarContractCallContractParam
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with StellarContractCallTrustLineParam
        try {
            StellarContractCallTrustLineParam.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for StellarContractCallTrustLineParam failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            // throw new IOException(String.format("The JSON string is invalid for StellarContractCallContractParam with oneOf schemas: StellarContractCallTrustLineParam. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of StellarContractCallContractParam given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of StellarContractCallContractParam
     * @throws IOException if the JSON string is invalid with respect to StellarContractCallContractParam
     */
    public static StellarContractCallContractParam fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, StellarContractCallContractParam.class);
    }

    /**
     * Convert an instance of StellarContractCallContractParam to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

