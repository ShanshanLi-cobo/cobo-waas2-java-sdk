/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.cobo.waas2.JSON;

/**
 * EstimatedSOLFeeSlow
 */
@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class EstimatedSOLFeeSlow {
  public static final String SERIALIZED_NAME_COMPUTE_UNIT_PRICE = "compute_unit_price";
  @SerializedName(SERIALIZED_NAME_COMPUTE_UNIT_PRICE)
  private String computeUnitPrice;

  public static final String SERIALIZED_NAME_COMPUTE_UNIT_LIMIT = "compute_unit_limit";
  @SerializedName(SERIALIZED_NAME_COMPUTE_UNIT_LIMIT)
  private String computeUnitLimit;

  public static final String SERIALIZED_NAME_BASE_FEE = "base_fee";
  @SerializedName(SERIALIZED_NAME_BASE_FEE)
  private String baseFee;

  public static final String SERIALIZED_NAME_RENT_AMOUNT = "rent_amount";
  @SerializedName(SERIALIZED_NAME_RENT_AMOUNT)
  private String rentAmount;

  public EstimatedSOLFeeSlow() {
  }

  public EstimatedSOLFeeSlow computeUnitPrice(String computeUnitPrice) {
    this.computeUnitPrice = computeUnitPrice;
    return this;
  }

   /**
   * The price paid per compute unit. This value determines the priority fee for the transaction, allowing you to increase inclusion probability in congested conditions.
   * @return computeUnitPrice
  **/
  @javax.annotation.Nonnull
  public String getComputeUnitPrice() {
    return computeUnitPrice;
  }

  public void setComputeUnitPrice(String computeUnitPrice) {
    this.computeUnitPrice = computeUnitPrice;
  }


  public EstimatedSOLFeeSlow computeUnitLimit(String computeUnitLimit) {
    this.computeUnitLimit = computeUnitLimit;
    return this;
  }

   /**
   * The maximum number of compute units your transaction is allowed to consume. It sets an upper bound on computational resource usage to prevent overload.
   * @return computeUnitLimit
  **/
  @javax.annotation.Nonnull
  public String getComputeUnitLimit() {
    return computeUnitLimit;
  }

  public void setComputeUnitLimit(String computeUnitLimit) {
    this.computeUnitLimit = computeUnitLimit;
  }


  public EstimatedSOLFeeSlow baseFee(String baseFee) {
    this.baseFee = baseFee;
    return this;
  }

   /**
   * A fixed fee charged per signature. The default is 5,000 lamports per signature.
   * @return baseFee
  **/
  @javax.annotation.Nonnull
  public String getBaseFee() {
    return baseFee;
  }

  public void setBaseFee(String baseFee) {
    this.baseFee = baseFee;
  }


  public EstimatedSOLFeeSlow rentAmount(String rentAmount) {
    this.rentAmount = rentAmount;
    return this;
  }

   /**
   * The rent fee charged by the network to store non–rent-exempt accounts on-chain. It is deducted periodically until the account maintains the minimum balance required for rent exemption.
   * @return rentAmount
  **/
  @javax.annotation.Nullable
  public String getRentAmount() {
    return rentAmount;
  }

  public void setRentAmount(String rentAmount) {
    this.rentAmount = rentAmount;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the EstimatedSOLFeeSlow instance itself
   */
  public EstimatedSOLFeeSlow putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    EstimatedSOLFeeSlow estimatedSOLFeeSlow = (EstimatedSOLFeeSlow) o;
    return Objects.equals(this.computeUnitPrice, estimatedSOLFeeSlow.computeUnitPrice) &&
        Objects.equals(this.computeUnitLimit, estimatedSOLFeeSlow.computeUnitLimit) &&
        Objects.equals(this.baseFee, estimatedSOLFeeSlow.baseFee) &&
        Objects.equals(this.rentAmount, estimatedSOLFeeSlow.rentAmount)&&
        Objects.equals(this.additionalProperties, estimatedSOLFeeSlow.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(computeUnitPrice, computeUnitLimit, baseFee, rentAmount, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class EstimatedSOLFeeSlow {\n");
    sb.append("    computeUnitPrice: ").append(toIndentedString(computeUnitPrice)).append("\n");
    sb.append("    computeUnitLimit: ").append(toIndentedString(computeUnitLimit)).append("\n");
    sb.append("    baseFee: ").append(toIndentedString(baseFee)).append("\n");
    sb.append("    rentAmount: ").append(toIndentedString(rentAmount)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("compute_unit_price");
    openapiFields.add("compute_unit_limit");
    openapiFields.add("base_fee");
    openapiFields.add("rent_amount");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("compute_unit_price");
    openapiRequiredFields.add("compute_unit_limit");
    openapiRequiredFields.add("base_fee");
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to EstimatedSOLFeeSlow
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!EstimatedSOLFeeSlow.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in EstimatedSOLFeeSlow is not found in the empty JSON string", EstimatedSOLFeeSlow.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : EstimatedSOLFeeSlow.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("compute_unit_price").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `compute_unit_price` to be a primitive type in the JSON string but got `%s`", jsonObj.get("compute_unit_price").toString()));
      }
      if (!jsonObj.get("compute_unit_limit").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `compute_unit_limit` to be a primitive type in the JSON string but got `%s`", jsonObj.get("compute_unit_limit").toString()));
      }
      if (!jsonObj.get("base_fee").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `base_fee` to be a primitive type in the JSON string but got `%s`", jsonObj.get("base_fee").toString()));
      }
      if ((jsonObj.get("rent_amount") != null && !jsonObj.get("rent_amount").isJsonNull()) && !jsonObj.get("rent_amount").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `rent_amount` to be a primitive type in the JSON string but got `%s`", jsonObj.get("rent_amount").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!EstimatedSOLFeeSlow.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'EstimatedSOLFeeSlow' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<EstimatedSOLFeeSlow> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(EstimatedSOLFeeSlow.class));

       return (TypeAdapter<T>) new TypeAdapter<EstimatedSOLFeeSlow>() {
           @Override
           public void write(JsonWriter out, EstimatedSOLFeeSlow value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() == null)
                   obj.addProperty(entry.getKey(), (String) null);
                 else if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public EstimatedSOLFeeSlow read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             EstimatedSOLFeeSlow instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of EstimatedSOLFeeSlow given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of EstimatedSOLFeeSlow
  * @throws IOException if the JSON string is invalid with respect to EstimatedSOLFeeSlow
  */
  public static EstimatedSOLFeeSlow fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, EstimatedSOLFeeSlow.class);
  }

 /**
  * Convert an instance of EstimatedSOLFeeSlow to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

