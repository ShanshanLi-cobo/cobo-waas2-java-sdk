/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.ApprovalEntry;
import com.cobo.waas2.model.ApprovalStatus;
import com.cobo.waas2.model.ApprovalUser;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.cobo.waas2.JSON;

/**
 * The information of an approval request detail.
 */
@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class ApprovalRequestDetail {
  public static final String SERIALIZED_NAME_APPROVAL_ID = "approval_id";
  @SerializedName(SERIALIZED_NAME_APPROVAL_ID)
  private String approvalId;

  public static final String SERIALIZED_NAME_INITIATOR = "initiator";
  @SerializedName(SERIALIZED_NAME_INITIATOR)
  private ApprovalUser initiator;

  public static final String SERIALIZED_NAME_APPROVED_LIST = "approved_list";
  @SerializedName(SERIALIZED_NAME_APPROVED_LIST)
  private List<ApprovalEntry> approvedList = new ArrayList<>();

  public static final String SERIALIZED_NAME_STATUS = "status";
  @SerializedName(SERIALIZED_NAME_STATUS)
  private ApprovalStatus status;

  public static final String SERIALIZED_NAME_MODIFIED_TIMESTAMP = "modified_timestamp";
  @SerializedName(SERIALIZED_NAME_MODIFIED_TIMESTAMP)
  private Long modifiedTimestamp;

  public ApprovalRequestDetail() {
  }

  public ApprovalRequestDetail approvalId(String approvalId) {
    this.approvalId = approvalId;
    return this;
  }

   /**
   * The system-generated unique ID of the approval request.
   * @return approvalId
  **/
  @javax.annotation.Nullable
  public String getApprovalId() {
    return approvalId;
  }

  public void setApprovalId(String approvalId) {
    this.approvalId = approvalId;
  }


  public ApprovalRequestDetail initiator(ApprovalUser initiator) {
    this.initiator = initiator;
    return this;
  }

   /**
   * Get initiator
   * @return initiator
  **/
  @javax.annotation.Nonnull
  public ApprovalUser getInitiator() {
    return initiator;
  }

  public void setInitiator(ApprovalUser initiator) {
    this.initiator = initiator;
  }


  public ApprovalRequestDetail approvedList(List<ApprovalEntry> approvedList) {
    this.approvedList = approvedList;
    return this;
  }

  public ApprovalRequestDetail addApprovedListItem(ApprovalEntry approvedListItem) {
    if (this.approvedList == null) {
      this.approvedList = new ArrayList<>();
    }
    this.approvedList.add(approvedListItem);
    return this;
  }

   /**
   * Get approvedList
   * @return approvedList
  **/
  @javax.annotation.Nullable
  public List<ApprovalEntry> getApprovedList() {
    return approvedList;
  }

  public void setApprovedList(List<ApprovalEntry> approvedList) {
    this.approvedList = approvedList;
  }


  public ApprovalRequestDetail status(ApprovalStatus status) {
    this.status = status;
    return this;
  }

   /**
   * Get status
   * @return status
  **/
  @javax.annotation.Nonnull
  public ApprovalStatus getStatus() {
    return status;
  }

  public void setStatus(ApprovalStatus status) {
    this.status = status;
  }


  public ApprovalRequestDetail modifiedTimestamp(Long modifiedTimestamp) {
    this.modifiedTimestamp = modifiedTimestamp;
    return this;
  }

   /**
   * The time when the approval was modified, in Unix timestamp format, measured in milliseconds.
   * @return modifiedTimestamp
  **/
  @javax.annotation.Nullable
  public Long getModifiedTimestamp() {
    return modifiedTimestamp;
  }

  public void setModifiedTimestamp(Long modifiedTimestamp) {
    this.modifiedTimestamp = modifiedTimestamp;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the ApprovalRequestDetail instance itself
   */
  public ApprovalRequestDetail putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    ApprovalRequestDetail approvalRequestDetail = (ApprovalRequestDetail) o;
    return Objects.equals(this.approvalId, approvalRequestDetail.approvalId) &&
        Objects.equals(this.initiator, approvalRequestDetail.initiator) &&
        Objects.equals(this.approvedList, approvalRequestDetail.approvedList) &&
        Objects.equals(this.status, approvalRequestDetail.status) &&
        Objects.equals(this.modifiedTimestamp, approvalRequestDetail.modifiedTimestamp)&&
        Objects.equals(this.additionalProperties, approvalRequestDetail.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(approvalId, initiator, approvedList, status, modifiedTimestamp, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class ApprovalRequestDetail {\n");
    sb.append("    approvalId: ").append(toIndentedString(approvalId)).append("\n");
    sb.append("    initiator: ").append(toIndentedString(initiator)).append("\n");
    sb.append("    approvedList: ").append(toIndentedString(approvedList)).append("\n");
    sb.append("    status: ").append(toIndentedString(status)).append("\n");
    sb.append("    modifiedTimestamp: ").append(toIndentedString(modifiedTimestamp)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("approval_id");
    openapiFields.add("initiator");
    openapiFields.add("approved_list");
    openapiFields.add("status");
    openapiFields.add("modified_timestamp");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("initiator");
    openapiRequiredFields.add("status");
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to ApprovalRequestDetail
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!ApprovalRequestDetail.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in ApprovalRequestDetail is not found in the empty JSON string", ApprovalRequestDetail.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : ApprovalRequestDetail.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("approval_id") != null && !jsonObj.get("approval_id").isJsonNull()) && !jsonObj.get("approval_id").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `approval_id` to be a primitive type in the JSON string but got `%s`", jsonObj.get("approval_id").toString()));
      }
      // validate the required field `initiator`
      ApprovalUser.validateJsonElement(jsonObj.get("initiator"));
      if (jsonObj.get("approved_list") != null && !jsonObj.get("approved_list").isJsonNull()) {
        JsonArray jsonArrayapprovedList = jsonObj.getAsJsonArray("approved_list");
        if (jsonArrayapprovedList != null) {
          // ensure the json data is an array
          if (!jsonObj.get("approved_list").isJsonArray()) {
            throw new IllegalArgumentException(String.format("Expected the field `approved_list` to be an array in the JSON string but got `%s`", jsonObj.get("approved_list").toString()));
          }

          // validate the optional field `approved_list` (array)
          for (int i = 0; i < jsonArrayapprovedList.size(); i++) {
            ApprovalEntry.validateJsonElement(jsonArrayapprovedList.get(i));
          };
        }
      }
      // validate the required field `status`
      ApprovalStatus.validateJsonElement(jsonObj.get("status"));
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!ApprovalRequestDetail.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'ApprovalRequestDetail' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<ApprovalRequestDetail> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(ApprovalRequestDetail.class));

       return (TypeAdapter<T>) new TypeAdapter<ApprovalRequestDetail>() {
           @Override
           public void write(JsonWriter out, ApprovalRequestDetail value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() == null)
                   obj.addProperty(entry.getKey(), (String) null);
                 else if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public ApprovalRequestDetail read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             ApprovalRequestDetail instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of ApprovalRequestDetail given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of ApprovalRequestDetail
  * @throws IOException if the JSON string is invalid with respect to ApprovalRequestDetail
  */
  public static ApprovalRequestDetail fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, ApprovalRequestDetail.class);
  }

 /**
  * Convert an instance of ApprovalRequestDetail to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

