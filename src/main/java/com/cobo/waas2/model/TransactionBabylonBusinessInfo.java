/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.AddressInfo;
import com.cobo.waas2.model.TransactionExtraType;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonArray;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.TypeAdapter;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;

import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;

import com.cobo.waas2.JSON;

/**
 * The Babylon business information.
 */
@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class TransactionBabylonBusinessInfo {
  public static final String SERIALIZED_NAME_EXTRA_TYPE = "extra_type";
  @SerializedName(SERIALIZED_NAME_EXTRA_TYPE)
  private TransactionExtraType extraType;

  public static final String SERIALIZED_NAME_BABYLON_ADDRESS_INFO = "babylon_address_info";
  @SerializedName(SERIALIZED_NAME_BABYLON_ADDRESS_INFO)
  private AddressInfo babylonAddressInfo;

  public static final String SERIALIZED_NAME_BTC_ADDRESS_INFO = "btc_address_info";
  @SerializedName(SERIALIZED_NAME_BTC_ADDRESS_INFO)
  private AddressInfo btcAddressInfo;

  public TransactionBabylonBusinessInfo() {
  }

  public TransactionBabylonBusinessInfo extraType(TransactionExtraType extraType) {
    this.extraType = extraType;
    return this;
  }

   /**
   * Get extraType
   * @return extraType
  **/
  @javax.annotation.Nonnull
  public TransactionExtraType getExtraType() {
    return extraType;
  }

  public void setExtraType(TransactionExtraType extraType) {
    this.extraType = extraType;
  }


  public TransactionBabylonBusinessInfo babylonAddressInfo(AddressInfo babylonAddressInfo) {
    this.babylonAddressInfo = babylonAddressInfo;
    return this;
  }

   /**
   * Get babylonAddressInfo
   * @return babylonAddressInfo
  **/
  @javax.annotation.Nullable
  public AddressInfo getBabylonAddressInfo() {
    return babylonAddressInfo;
  }

  public void setBabylonAddressInfo(AddressInfo babylonAddressInfo) {
    this.babylonAddressInfo = babylonAddressInfo;
  }


  public TransactionBabylonBusinessInfo btcAddressInfo(AddressInfo btcAddressInfo) {
    this.btcAddressInfo = btcAddressInfo;
    return this;
  }

   /**
   * Get btcAddressInfo
   * @return btcAddressInfo
  **/
  @javax.annotation.Nullable
  public AddressInfo getBtcAddressInfo() {
    return btcAddressInfo;
  }

  public void setBtcAddressInfo(AddressInfo btcAddressInfo) {
    this.btcAddressInfo = btcAddressInfo;
  }

  /**
   * A container for additional, undeclared properties.
   * This is a holder for any undeclared properties as specified with
   * the 'additionalProperties' keyword in the OAS document.
   */
  private Map<String, Object> additionalProperties;

  /**
   * Set the additional (undeclared) property with the specified name and value.
   * If the property does not already exist, create it otherwise replace it.
   *
   * @param key name of the property
   * @param value value of the property
   * @return the TransactionBabylonBusinessInfo instance itself
   */
  public TransactionBabylonBusinessInfo putAdditionalProperty(String key, Object value) {
    if (this.additionalProperties == null) {
        this.additionalProperties = new HashMap<String, Object>();
    }
    this.additionalProperties.put(key, value);
    return this;
  }

  /**
   * Return the additional (undeclared) property.
   *
   * @return a map of objects
   */
  public Map<String, Object> getAdditionalProperties() {
    return additionalProperties;
  }

  /**
   * Return the additional (undeclared) property with the specified name.
   *
   * @param key name of the property
   * @return an object
   */
  public Object getAdditionalProperty(String key) {
    if (this.additionalProperties == null) {
        return null;
    }
    return this.additionalProperties.get(key);
  }


  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TransactionBabylonBusinessInfo transactionBabylonBusinessInfo = (TransactionBabylonBusinessInfo) o;
    return Objects.equals(this.extraType, transactionBabylonBusinessInfo.extraType) &&
        Objects.equals(this.babylonAddressInfo, transactionBabylonBusinessInfo.babylonAddressInfo) &&
        Objects.equals(this.btcAddressInfo, transactionBabylonBusinessInfo.btcAddressInfo)&&
        Objects.equals(this.additionalProperties, transactionBabylonBusinessInfo.additionalProperties);
  }

  @Override
  public int hashCode() {
    return Objects.hash(extraType, babylonAddressInfo, btcAddressInfo, additionalProperties);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TransactionBabylonBusinessInfo {\n");
    sb.append("    extraType: ").append(toIndentedString(extraType)).append("\n");
    sb.append("    babylonAddressInfo: ").append(toIndentedString(babylonAddressInfo)).append("\n");
    sb.append("    btcAddressInfo: ").append(toIndentedString(btcAddressInfo)).append("\n");
    sb.append("    additionalProperties: ").append(toIndentedString(additionalProperties)).append("\n");
    sb.append("}");
    return sb.toString();
  }

  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("extra_type");
    openapiFields.add("babylon_address_info");
    openapiFields.add("btc_address_info");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("extra_type");
  }

 /**
  * Validates the JSON Element and throws an exception if issues found
  *
  * @param jsonElement JSON Element
  * @throws IOException if the JSON Element is invalid with respect to TransactionBabylonBusinessInfo
  */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TransactionBabylonBusinessInfo.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TransactionBabylonBusinessInfo is not found in the empty JSON string", TransactionBabylonBusinessInfo.openapiRequiredFields.toString()));
        }
      }

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : TransactionBabylonBusinessInfo.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the required field `extra_type`
      TransactionExtraType.validateJsonElement(jsonObj.get("extra_type"));
      // validate the optional field `babylon_address_info`
      if (jsonObj.get("babylon_address_info") != null && !jsonObj.get("babylon_address_info").isJsonNull()) {
        AddressInfo.validateJsonElement(jsonObj.get("babylon_address_info"));
      }
      // validate the optional field `btc_address_info`
      if (jsonObj.get("btc_address_info") != null && !jsonObj.get("btc_address_info").isJsonNull()) {
        AddressInfo.validateJsonElement(jsonObj.get("btc_address_info"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TransactionBabylonBusinessInfo.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TransactionBabylonBusinessInfo' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TransactionBabylonBusinessInfo> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TransactionBabylonBusinessInfo.class));

       return (TypeAdapter<T>) new TypeAdapter<TransactionBabylonBusinessInfo>() {
           @Override
           public void write(JsonWriter out, TransactionBabylonBusinessInfo value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             obj.remove("additionalProperties");
             // serialize additional properties
             if (value.getAdditionalProperties() != null) {
               for (Map.Entry<String, Object> entry : value.getAdditionalProperties().entrySet()) {
                 if (entry.getValue() == null)
                   obj.addProperty(entry.getKey(), (String) null);
                 else if (entry.getValue() instanceof String)
                   obj.addProperty(entry.getKey(), (String) entry.getValue());
                 else if (entry.getValue() instanceof Number)
                   obj.addProperty(entry.getKey(), (Number) entry.getValue());
                 else if (entry.getValue() instanceof Boolean)
                   obj.addProperty(entry.getKey(), (Boolean) entry.getValue());
                 else if (entry.getValue() instanceof Character)
                   obj.addProperty(entry.getKey(), (Character) entry.getValue());
                 else {
                   JsonElement jsonElement = gson.toJsonTree(entry.getValue());
                   if (jsonElement.isJsonArray()) {
                     obj.add(entry.getKey(), jsonElement.getAsJsonArray());
                   } else {
                     obj.add(entry.getKey(), jsonElement.getAsJsonObject());
                   }
                 }
               }
             }
             elementAdapter.write(out, obj);
           }

           @Override
           public TransactionBabylonBusinessInfo read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             JsonObject jsonObj = jsonElement.getAsJsonObject();
             // store additional fields in the deserialized instance
             TransactionBabylonBusinessInfo instance = thisAdapter.fromJsonTree(jsonObj);
             for (Map.Entry<String, JsonElement> entry : jsonObj.entrySet()) {
               if (!openapiFields.contains(entry.getKey())) {
                 if (entry.getValue().isJsonPrimitive()) { // primitive type
                   if (entry.getValue().getAsJsonPrimitive().isString())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsString());
                   else if (entry.getValue().getAsJsonPrimitive().isNumber())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsNumber());
                   else if (entry.getValue().getAsJsonPrimitive().isBoolean())
                     instance.putAdditionalProperty(entry.getKey(), entry.getValue().getAsBoolean());
                   else
                     throw new IllegalArgumentException(String.format("The field `%s` has unknown primitive type. Value: %s", entry.getKey(), entry.getValue().toString()));
                 } else if (entry.getValue().isJsonArray()) {
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), List.class));
                 } else { // JSON object
                     instance.putAdditionalProperty(entry.getKey(), gson.fromJson(entry.getValue(), HashMap.class));
                 }
               }
             }
             return instance;
           }

       }.nullSafe();
    }
  }

 /**
  * Create an instance of TransactionBabylonBusinessInfo given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of TransactionBabylonBusinessInfo
  * @throws IOException if the JSON string is invalid with respect to TransactionBabylonBusinessInfo
  */
  public static TransactionBabylonBusinessInfo fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TransactionBabylonBusinessInfo.class);
  }

 /**
  * Convert an instance of TransactionBabylonBusinessInfo to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

