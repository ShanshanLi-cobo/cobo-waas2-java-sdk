/*
 * Cobo Wallet as a Service 2.0
 *
 * Contact: help@cobo.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.cobo.waas2.model;

import java.util.Objects;
import com.cobo.waas2.model.TSSEventDataType;
import com.cobo.waas2.model.TSSKeyGenEventData;
import com.cobo.waas2.model.TSSKeyReshareEventData;
import com.cobo.waas2.model.TSSKeyShareSignEventData;
import com.cobo.waas2.model.TSSKeyShareSignRequest;
import com.cobo.waas2.model.TSSKeyShareSignSignatures;
import com.cobo.waas2.model.TSSKeySignEventData;
import com.cobo.waas2.model.TSSRequestTypeEenum;
import com.cobo.waas2.model.TSSStatus;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;



import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonArray;
import com.google.gson.JsonParseException;

import com.cobo.waas2.JSON;

@javax.annotation.Generated(
    value = "org.openapitools.codegen.languages.JavaClientCodegen", 
    comments = "Generator version: 7.6.0"
)
public class TSSEventData extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(TSSEventData.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!TSSEventData.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'TSSEventData' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<TSSKeyGenEventData> adapterTSSKeyGenEventData = gson.getDelegateAdapter(this, TypeToken.get(TSSKeyGenEventData.class));
            final TypeAdapter<TSSKeyReshareEventData> adapterTSSKeyReshareEventData = gson.getDelegateAdapter(this, TypeToken.get(TSSKeyReshareEventData.class));
            final TypeAdapter<TSSKeySignEventData> adapterTSSKeySignEventData = gson.getDelegateAdapter(this, TypeToken.get(TSSKeySignEventData.class));
            final TypeAdapter<TSSKeyShareSignEventData> adapterTSSKeyShareSignEventData = gson.getDelegateAdapter(this, TypeToken.get(TSSKeyShareSignEventData.class));

            return (TypeAdapter<T>) new TypeAdapter<TSSEventData>() {
                @Override
                public void write(JsonWriter out, TSSEventData value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `TSSKeyGenEventData`
                    if (value.getActualInstance() instanceof TSSKeyGenEventData) {
                        JsonElement element = adapterTSSKeyGenEventData.toJsonTree((TSSKeyGenEventData)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `TSSKeyReshareEventData`
                    if (value.getActualInstance() instanceof TSSKeyReshareEventData) {
                        JsonElement element = adapterTSSKeyReshareEventData.toJsonTree((TSSKeyReshareEventData)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `TSSKeySignEventData`
                    if (value.getActualInstance() instanceof TSSKeySignEventData) {
                        JsonElement element = adapterTSSKeySignEventData.toJsonTree((TSSKeySignEventData)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    // check if the actual instance is of the type `TSSKeyShareSignEventData`
                    if (value.getActualInstance() instanceof TSSKeyShareSignEventData) {
                        JsonElement element = adapterTSSKeyShareSignEventData.toJsonTree((TSSKeyShareSignEventData)value.getActualInstance());
                        elementAdapter.write(out, element);
                        return;
                    }
                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData");
                }

                @Override
                public TSSEventData read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonElement jsonElement = elementAdapter.read(in);

                    JsonObject jsonObject = jsonElement.getAsJsonObject();

                    // use discriminator value for faster oneOf lookup
                    TSSEventData newTSSEventData = new TSSEventData();
                    if (jsonObject.get("data_type") == null) {
                        log.log(Level.WARNING, "Failed to lookup discriminator value for TSSEventData as `data_type` was not found in the payload or the payload is empty.");
                    } else  {
                        // look up the discriminator value in the field `data_type`
                        switch (jsonObject.get("data_type").getAsString()) {
                            case "KeyGen":
                                deserialized = adapterTSSKeyGenEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "KeyReshare":
                                deserialized = adapterTSSKeyReshareEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "KeyShareSign":
                                deserialized = adapterTSSKeyShareSignEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "KeySign":
                                deserialized = adapterTSSKeySignEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "TSSKeyGenEventData":
                                deserialized = adapterTSSKeyGenEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "TSSKeyReshareEventData":
                                deserialized = adapterTSSKeyReshareEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "TSSKeyShareSignEventData":
                                deserialized = adapterTSSKeyShareSignEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            case "TSSKeySignEventData":
                                deserialized = adapterTSSKeySignEventData.fromJsonTree(jsonObject);
                                newTSSEventData.setActualInstance(deserialized);
                                return newTSSEventData;
                            default:
                                log.log(Level.WARNING, String.format("Failed to lookup discriminator value `%s` for TSSEventData. Possible values: KeyGen KeyReshare KeyShareSign KeySign TSSKeyGenEventData TSSKeyReshareEventData TSSKeyShareSignEventData TSSKeySignEventData", jsonObject.get("data_type").getAsString()));
                        }
                    }

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize TSSKeyGenEventData
                    try {
                        // validate the JSON object to see if any exception is thrown
                        TSSKeyGenEventData.validateJsonElement(jsonElement);
                        actualAdapter = adapterTSSKeyGenEventData;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'TSSKeyGenEventData'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for TSSKeyGenEventData failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'TSSKeyGenEventData'", e);
                    }
                    // deserialize TSSKeyReshareEventData
                    try {
                        // validate the JSON object to see if any exception is thrown
                        TSSKeyReshareEventData.validateJsonElement(jsonElement);
                        actualAdapter = adapterTSSKeyReshareEventData;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'TSSKeyReshareEventData'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for TSSKeyReshareEventData failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'TSSKeyReshareEventData'", e);
                    }
                    // deserialize TSSKeySignEventData
                    try {
                        // validate the JSON object to see if any exception is thrown
                        TSSKeySignEventData.validateJsonElement(jsonElement);
                        actualAdapter = adapterTSSKeySignEventData;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'TSSKeySignEventData'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for TSSKeySignEventData failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'TSSKeySignEventData'", e);
                    }
                    // deserialize TSSKeyShareSignEventData
                    try {
                        // validate the JSON object to see if any exception is thrown
                        TSSKeyShareSignEventData.validateJsonElement(jsonElement);
                        actualAdapter = adapterTSSKeyShareSignEventData;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'TSSKeyShareSignEventData'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for TSSKeyShareSignEventData failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'TSSKeyShareSignEventData'", e);
                    }

                    if (match == 1) {
                        TSSEventData ret = new TSSEventData();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonElement));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for TSSEventData: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonElement.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<String, Class<?>>();

    public TSSEventData() {
        super("oneOf", Boolean.FALSE);
    }

    public TSSEventData(TSSKeyGenEventData o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public TSSEventData(TSSKeyReshareEventData o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public TSSEventData(TSSKeyShareSignEventData o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public TSSEventData(TSSKeySignEventData o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("TSSKeyGenEventData", TSSKeyGenEventData.class);
        schemas.put("TSSKeyReshareEventData", TSSKeyReshareEventData.class);
        schemas.put("TSSKeySignEventData", TSSKeySignEventData.class);
        schemas.put("TSSKeyShareSignEventData", TSSKeyShareSignEventData.class);
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return TSSEventData.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData
     *
     * It could be an instance of the 'oneOf' schemas.
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof TSSKeyGenEventData) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof TSSKeyReshareEventData) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof TSSKeySignEventData) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof TSSKeyShareSignEventData) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData");
    }

    /**
     * Get the actual instance, which can be the following:
     * TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData
     *
     * @return The actual instance (TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData)
     */
    @SuppressWarnings("unchecked")
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `TSSKeyGenEventData`. If the actual instance is not `TSSKeyGenEventData`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TSSKeyGenEventData`
     * @throws ClassCastException if the instance is not `TSSKeyGenEventData`
     */
    public TSSKeyGenEventData getTSSKeyGenEventData() throws ClassCastException {
        return (TSSKeyGenEventData)super.getActualInstance();
    }
    /**
     * Get the actual instance of `TSSKeyReshareEventData`. If the actual instance is not `TSSKeyReshareEventData`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TSSKeyReshareEventData`
     * @throws ClassCastException if the instance is not `TSSKeyReshareEventData`
     */
    public TSSKeyReshareEventData getTSSKeyReshareEventData() throws ClassCastException {
        return (TSSKeyReshareEventData)super.getActualInstance();
    }
    /**
     * Get the actual instance of `TSSKeySignEventData`. If the actual instance is not `TSSKeySignEventData`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TSSKeySignEventData`
     * @throws ClassCastException if the instance is not `TSSKeySignEventData`
     */
    public TSSKeySignEventData getTSSKeySignEventData() throws ClassCastException {
        return (TSSKeySignEventData)super.getActualInstance();
    }
    /**
     * Get the actual instance of `TSSKeyShareSignEventData`. If the actual instance is not `TSSKeyShareSignEventData`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TSSKeyShareSignEventData`
     * @throws ClassCastException if the instance is not `TSSKeyShareSignEventData`
     */
    public TSSKeyShareSignEventData getTSSKeyShareSignEventData() throws ClassCastException {
        return (TSSKeyShareSignEventData)super.getActualInstance();
    }

    /**
     * Validates the JSON Element and throws an exception if issues found
     *
     * @param jsonElement JSON Element
     * @throws IOException if the JSON Element is invalid with respect to TSSEventData
     */
    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
        // validate oneOf schemas one by one
        int validCount = 0;
        ArrayList<String> errorMessages = new ArrayList<>();
        // validate the json string with TSSKeyGenEventData
        try {
            TSSKeyGenEventData.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for TSSKeyGenEventData failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with TSSKeyReshareEventData
        try {
            TSSKeyReshareEventData.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for TSSKeyReshareEventData failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with TSSKeySignEventData
        try {
            TSSKeySignEventData.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for TSSKeySignEventData failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        // validate the json string with TSSKeyShareSignEventData
        try {
            TSSKeyShareSignEventData.validateJsonElement(jsonElement);
            validCount++;
        } catch (Exception e) {
            errorMessages.add(String.format("Deserialization for TSSKeyShareSignEventData failed with `%s`.", e.getMessage()));
            // continue to the next one
        }
        if (validCount != 1) {
            // throw new IOException(String.format("The JSON string is invalid for TSSEventData with oneOf schemas: TSSKeyGenEventData, TSSKeyReshareEventData, TSSKeyShareSignEventData, TSSKeySignEventData. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonElement.toString()));
        }
    }

    /**
     * Create an instance of TSSEventData given an JSON string
     *
     * @param jsonString JSON string
     * @return An instance of TSSEventData
     * @throws IOException if the JSON string is invalid with respect to TSSEventData
     */
    public static TSSEventData fromJson(String jsonString) throws IOException {
        return JSON.getGson().fromJson(jsonString, TSSEventData.class);
    }

    /**
     * Convert an instance of TSSEventData to an JSON string
     *
     * @return JSON string
     */
    public String toJson() {
        return JSON.getGson().toJson(this);
    }
}

